# 구독 전환 설계 — 3일 무료 체험 → 유료 구독

기존 [DESIGN.md](DESIGN.md)가 시각 디자인 시스템을 다루듯, 이 문서는 "무료 체험 → 결제 전환" 퍼널의
행동 설계를 다룬다. 확정된 두 가지 결정을 전제로 한다.

- **체험 방식**: 카드 등록 후 체험 (체험 종료 시 자동으로 유료 전환·결제)
- **체험 기간**: 3일

원칙은 하나다. **사용자가 지금 보는 정보는 전부 사실이어야 한다.** 남은 일수, 다음 결제일과 금액,
해지 방법, 지금 이 앱으로 무엇을 할 수 있는지 — 전부 실제 상태와 정확히 일치해야 한다. 아래 모든 설계는
이 제약 안에서 전환율을 올리는 방법이고, 마지막 절에는 사실이 아니면 성립하지 않아서 **의도적으로 제외한
기법**을 따로 정리했다.

---

## 1. 왜 이 모델인가

3일 카드 등록형 체험을 고른 이유:

- SoundNative의 온보딩(퀴즈 → 상황 → 레벨 → 빈도 → 목표, [App.tsx:119-152](src/App.tsx#L119-L152))은
  이미 사용자의 상황을 파악하고 "지금 무슨 말인지 모르겠나요? → Decode 열기"([HomeScreen.tsx:230-239](src/HomeScreen.tsx#L230-L239))
  같은 **Aha! 모먼트**를 3일 안에 충분히 보여줄 수 있는 구조다.
- 카드 등록형은 "나중에 다시 결제 정보 입력" 이탈 지점이 없어 전환율이 높다. 대신 **몰래 결제** 논란을
  피하려면 시작 시점부터 종료 시점까지 금액·날짜 고지가 매우 명확해야 한다 — 이건 뒤에서 다룬다.
- Polar는 체험 종료 전 리마인드 이메일을 자동 발송한다 (Trials — polar.sh/docs/features/subscriptions/trials).
  이건 "서프라이즈 결제"를 막는 안전장치이자, 아래 4단계(체험 종료 임박)의 핵심 채널이다.

---

## 2. 지금 없는 것: Entitlement(이용 권한) 모델

현재 상태를 명확히 짚어야 설계가 붕 뜨지 않는다:

- [polar-webhook.ts:65-71](functions/api/polar-webhook.ts#L65-L71)의 주석 그대로, 결제 이벤트를 로그만 찍고
  **어디에도 저장하지 않는다.** 유저 레코드 자체가 없다 (Supabase Auth로 로그인은 하지만 구독 상태를 붙일 테이블이 없음).
- [App.tsx](src/App.tsx)에는 페이월 게이팅이 없다. `pricing` 화면은 온보딩 중 한 번 보여주고 건너뛸 수 있는
  화면일 뿐, 홈/퀘스트/Decode/노트는 결제 여부와 무관하게 전부 열려 있다.
- 즉 지금은 "체험"도 "구독 필수"도 개념 자체가 존재하지 않는다. 이걸 만드는 게 이 설계의 실질적인 절반이다.

### 필요한 스키마 (Supabase)

```sql
create table subscriptions (
  user_id uuid primary key references auth.users(id) on delete cascade,
  polar_customer_id text not null,
  polar_subscription_id text not null unique,
  status text not null check (status in ('trialing','active','past_due','canceled','revoked')),
  trial_ends_at timestamptz,
  current_period_end timestamptz,
  cancel_at_period_end boolean not null default false,
  updated_at timestamptz not null default now()
);
-- RLS: 유저는 자기 행만 select. insert/update는 서비스 롤(webhook)만.
```

`status`는 Polar가 웹훅으로 보내는 이벤트를 그대로 반영한다 (`subscription.active`, `subscription.canceled`
→ `cancel_at_period_end = true`이지만 기간 끝까지는 `status = 'active'` 유지, `subscription.revoked` → 즉시
접근 차단). **`trialing`과 `active`를 구분해서 저장해야** 3단계·4단계의 "진짜 남은 일수" 카피가 가능해진다.

### 게이팅 지점

- `App.tsx`에 `useEntitlement()` 훅 추가 → `home`/`quest`/`decode`/`notes` 진입 전에 상태 확인.
- 상태가 `trialing`/`active`이면 통과. 그 외(`canceled` + 기간 만료, `revoked`, 레코드 없음)면 `pricing` 화면으로
  강제 이동. 지금처럼 "나중에 하기"로 건너뛸 수 있는 스킵 버튼은 **체험 중이거나 활성 구독 중일 때만** 노출한다
  (건너뛸 게 없는데 건너뛰기 버튼을 보여주는 건 거짓 정보는 아니지만 혼란을 준다).
- 신규가입 온보딩 흐름에서 `auth` 이후 화면을 `pricing`(현재) → **`trialStart`**로 교체. 여기서 카드를
  등록하면 체험이 시작되고 바로 `home`으로 간다. 카드 등록을 건너뛰면 홈에 못 들어가는 게 원칙(하드 페이월)이므로,
  이 화면엔 "나중에" 버튼을 두지 않는다 — 대신 **왜 카드가 필요한지, 언제 얼마가 청구되는지**를 화면 안에서
  다 보여준다 (3단계 카피 참고).

---

## 3. 퍼널 단계별 심리 원칙 매핑

사용자가 제공한 80여 개 원칙 중, "사실을 왜곡하지 않고 그대로 쓸 수 있는" 것만 골라 퍼널 단계에 배치했다.
각 항목은 **무엇을·왜·구체적으로 어떻게**를 담는다.

### 0단계 — 온보딩 (가입 전, 페이월 도달 전)

이미 구현된 흐름을 그대로 활용하되 의도를 명확히 한다.

| 원칙 | 적용 |
|---|---|
| **목표 그라데이션 효과** | Step 1~5 진행 바([StatusScreen.tsx:67-77](src/StatusScreen.tsx#L67-L77) 이미 있음)를 전 온보딩 화면에 일관 노출 — 이미 되어 있음, 유지. |
| **이케아 효과** | 상황/레벨/빈도/목표를 직접 선택하게 함으로써 "내가 설정한 학습 계획"이라는 소유감을 만듦 — 이미 구조상 있음. |
| **가용성 휴리스틱 / 아하! 모먼트** | 유료 게이트 전에 TeaserQuiz로 실제 문제를 하나 풀게 해서 "내가 못 알아듣는 부분이 있구나"를 체감시킨다 — TeaserQuiz.tsx 이미 존재, 순서 유지가 핵심. |
| **힉스의 법칙** | 온보딩 각 화면은 선택지 4~5개로 제한(StatusScreen 이미 그렇게 되어 있음) — 결정 피로를 줄여 이탈 방지. |
| **프레이밍** | "학습을 시작하세요"가 아니라 "[선택한 목표]를 [선택한 빈도]로 준비하세요"처럼 사용자가 입력한 데이터를 그대로 반영한 문구 사용. |

### 1단계 — 체험 시작 화면 (`trialStart`, 회원가입 직후)

여기가 가장 중요하고 가장 조심해야 할 지점이다. "카드 등록 후 체험"은 정직하게 설계하지 않으면 그 자체로
다크패턴이 된다.

| 원칙 | 적용 | 정직성 조건 |
|---|---|---|
| **손실 회피 (Loss Aversion)** | "지금 시작하면 3일 동안 Decode·전체 퀘스트·노트 저장을 다 써볼 수 있어요"처럼 **얻는 것**을 프레이밍하되, 체험이 끝나면 잃는 게 뭔지도 같은 화면에 명시 (아래 참고). | 체험이 끝났을 때 실제로 잠기는 기능만 나열. |
| **앵커링** | `/api/pricing`에서 받아온 실제 월/연 가격을 체험 시작 화면에도 미리 보여준다 ("3일 후 {가격}부터 청구돼요"). | 화면에 보이는 숫자와 결제 시점 실제 청구액이 100% 일치해야 함 — Polar 응답을 그대로 렌더링하는 지금 PricingScreen 방식 유지. |
| **포카요케 (Poka-yoke)** | "결제 예정일: 2026년 8월 1일 · 청구 금액: ₩X,XXX · 이 화면을 벗어나도 체험은 계속돼요" 같은 문구를 카드 입력 폼 바로 위, 결제 버튼 근처에 배치해서 **실수로 놓칠 수 없게** 한다. | 이게 이 화면 전체에서 가장 중요한 한 줄 — 숨기거나 작게 쓰면 안 됨. |
| **지시자 효과 (Signifiers)** | 버튼 라벨은 "구독하기"가 아니라 "3일 무료로 시작하기"로 — 지금 이 순간 돈이 나가지 않는다는 사실을 버튼 텍스트 자체로 전달. | 실제로 지금 이 순간엔 청구되지 않아야 함 (Polar 체험 설정과 일치). |
| **사회적 증거 (Social Proof)** | 실제 사용자 수·완주율 같은 실측 지표가 쌓이면 노출 (예: "이번 달 체험을 시작한 유학생 N명"). | **지금은 데이터가 없으므로 넣지 않는다.** 숫자를 지어내는 순간 원칙 위반. |

### 2단계 — 체험 기간 중 (Day 0~3)

체험 중 사용자가 실제로 가치를 경험하게 만드는 것 자체가 최고의 전환 장치다.

| 원칙 | 적용 |
|---|---|
| **제이가르닉 효과** | 홈 화면의 학습 경로(마디 체인, [HomeScreen.tsx:140-223](src/HomeScreen.tsx#L140-L223))는 이미 "완료/오늘/미래" 상태를 시각화 — 미완료 마디가 남아있는 느낌을 그대로 유지. |
| **피드백 루프 / 스파크 효과** | 스트릭(`streak`)과 저장된 노트 수를 상단에 실시간 표시 — 이미 있음([HomeScreen.tsx:81-90](src/HomeScreen.tsx#L81-L90)), 유지. |
| **내부 트리거 형성** | 체험 중 "오늘의 퀘스트"를 완료하면 앱 밖에서도 떠올릴 만한 짧은 요약(예: 오늘 배운 표현 1개)을 노트에 저장 — 이미 있는 노트 기능을 체험 기간에 강조. |
| **목표 그라데이션 효과** | "체험 D-2, 지금까지 퀘스트 3/10 완료"처럼 **남은 일수와 실제 진행률을 함께** 보여주는 배너를 홈 상단에 추가. 진행률이 올라갈수록 완주 욕구가 커짐. |
| **유혹 번들링** | "출퇴근길 5분" 같은 기존 사용자 목표(FrequencyScreen에서 이미 입력받음)와 오늘의 퀘스트를 연결한 알림 문구. |

### 3단계 — 체험 종료 임박 (Day 2~3, 전환 직전)

가장 정직성이 시험받는 구간. **Polar의 자동 리마인드 이메일**을 기본 채널로 쓰고, 앱 내 배너로 보강한다.

| 원칙 | 적용 |
|---|---|
| **손실 회피** | "체험이 8월 1일에 끝나요. 이후엔 Decode와 새 퀘스트가 잠겨요"처럼, 지금 쌓은 것(스트릭·노트)이 아니라 **앞으로 못 쓰게 되는 기능**을 구체적으로 명시. |
| **초월 할인(Hyperbolic Discounting) 대응** | "3일 후"라는 먼 미래를 흐릿하게 인식하는 성향을 상쇄하기 위해, 정확한 날짜+요일("8월 1일 토요일")과 함께 홈 진입 시 눈에 띄는 배너로 표시 — 이메일 하나에 의존하지 않음. |
| **포카요케** | 배너 클릭 시 "지금 해지하면 결제 안 돼요" 버튼을 **같은 화면, 같은 무게감**으로 노출 (다음 절 "해지" 참고). 결제 버튼만 크게 하고 해지 경로를 숨기지 않는다. |
| **프레이밍** | 월 결제라면 "하루 약 {가격/30}원"처럼 실제 계산값으로 체감 비용을 낮춰 보여준다 — 반올림·과장 없이 정확한 나눗셈. |

### 4단계 — 전환 시점 (자동 결제 발생)

체험이 끝나고 실제 첫 결제가 일어나는 순간. Polar가 처리하지만, 그 직후 사용자가 앱에 돌아왔을 때의 경험을 설계한다.

| 원칙 | 적용 |
|---|---|
| **오컴의 면도날** | 지금 상품이 1개(단일 요금제)이므로 미끼 효과(디코이)를 억지로 만들지 않는다. 나중에 연간 요금제를 추가하면 그때 "월간 대비 연간 절약액"을 실제 계산값으로 보여주는 것으로 충분 — 가짜 "인기" 배지 등은 쓰지 않는다. |
| **보답 기대 효과 (Reciprocity)** | 첫 결제 완료 직후 화면([CheckoutSuccessScreen.tsx](src/CheckoutSuccessScreen.tsx))에서 실제로 줄 수 있는 것(예: 그동안 배운 표현 전체를 정리한 노트 이메일 발송 — 이미 `send-notes-email.ts` 존재)을 언급. |
| **인식 우선 원칙** | 결제 후 홈 화면에서 "이제 전부 열렸어요"를 배지/잠금 해제 애니메이션으로 즉시 보여줘 — 결제했다는 사실을 기억에 의존하게 하지 않음. |

### 5단계 — 결제 후 지속 사용 (리텐션)

| 원칙 | 적용 |
|---|---|
| **이케아 효과 / 디드로 효과** | 노트가 쌓일수록("지금까지 배운 표현 N개") 해지 시 그 자산을 잃는다는 감각이 자연히 생김 — 인위적 조작 없이 실사용 데이터로 성립. |
| **일관성/책임감 (Commitment & Consistency)** | 온보딩에서 스스로 설정한 목표·빈도를 마이페이지나 홈에서 주기적으로 다시 보여줘 ("워킹홀리데이 준비, 주 5회 목표로 시작하셨어요") — 본인이 한 선택을 상기시키는 것뿐, 조작이 아님. |

### 6단계 — 해지·환불

가장 정직성이 요구되는 지점이자, 이 프로젝트가 이미 잘하고 있는 패턴(회원 탈퇴 확인 UX, [MyPageScreen.tsx:212-271](src/MyPageScreen.tsx#L212-L271))을 참고할 수 있다.

- 마이페이지에 "구독 관리" 섹션 신설: 현재 상태(체험 중/활성/해지 예정), 다음 결제일과 금액, **"구독 해지" 버튼을
  가입만큼 쉬운 클릭 수로** 배치.
- 해지 확인 문구는 사실만: "지금 해지하면 {현재 결제 주기 종료일}까지는 계속 이용할 수 있고, 이후 자동
  결제되지 않아요." — 죄책감을 유발하는 문구("정말 포기하시는 건가요?" 같은 confirmshaming)는 쓰지 않는다.
- Polar Customer Portal(고객이 직접 결제수단·구독 상태를 관리하는 Polar 제공 화면)로 딥링크하는 것도 방법 —
  자체 UI를 새로 만들 필요 없이 "쉬운 해지"를 보장할 수 있다.

---

## 4. 의도적으로 배제한 기법

목록에 있던 원칙 중, **사실 그대로는 못 쓰거나 쓰면 안 되는 것들**을 명시적으로 뺐다. 이유를 남겨서
나중에 "왜 이건 안 썼지"라는 질문이 안 나오게 한다.

- **결핍 효과 / 특이성 효과 (가짜 한정)** — "지금 가입하면 특별 혜택" 같은 카피는 실제로 한정된 게 없으면 거짓. 재고·인원·시간이 실제로 제한될 때만 쓴다. 지금은 쓸 근거가 없음.
- **밴드웨건 효과 / 사회적 증거의 가짜 수치** — "1만 명이 선택한" 같은 문구는 실측 데이터가 쌓이기 전엔 지어낼 수밖에 없어 제외. 데이터 확보 후 재검토.
- **미끼 효과 (Decoy Effect)** — 지금은 단일 상품이라 적용 대상 자체가 없음. 다중 요금제 도입 시, 각 티어가 실제로 다른 가치를 제공할 때만 사용 (허수 티어 금지).
- **확인수치심(confirmshaming, 목록엔 없지만 반발감 효과의 반대 오남용 사례)** — 해지 버튼 옆에 "아니요, 계속 실력 없이 살래요" 같은 문구를 넣는 패턴. 반발감(Reactance)을 이해한다는 것과 그걸 이용해 조롱하는 건 다르다.
- **스트라이샌드 효과** — 이건 애초에 "쓸 기법"이 아니라 경고다: 부정적 후기나 해지 관련 불만을 숨기거나 삭제하려 하면 역풍을 부른다. 그래서 해지를 쉽게 만드는 쪽(6단계)이 장기적으로 안전하다.
- **가짜 긴급성(카운트다운 타이머 재생성, 목표 그라데이션 오남용)** — 체험 종료일은 실제 날짜를 고정 표시하고, 페이지를 새로고침할 때마다 타이머가 리셋되는 식의 가짜 긴급성은 만들지 않는다.
- **디폴트 편향 오남용** — 체크아웃 화면에서 연간 요금제나 부가 상품을 기본 선택해두고 사용자가 못 알아채게 하는 방식은 안 씀 (도입 시 옵션이 생기면 기본값도 명확히 드러나게 배치).

---

## 5. 구현 체크리스트

설계이므로 실제 코드 변경은 별도 작업이지만, 다음 순서를 제안한다.

1. Supabase `subscriptions` 테이블 + RLS 정책 추가.
2. `functions/api/checkout.ts`에 Polar 체험 파라미터 추가 (체험 3일 설정 — Product/Price 단에서 설정하는 게 Polar 권장 방식이므로 Polar 대시보드에서 우선 확인).
3. `functions/api/polar-webhook.ts`에서 `subscription.*` 이벤트를 받아 `subscriptions` 테이블에 upsert (지금은 로그만 찍는 상태).
4. `App.tsx`에 entitlement 훅 + 게이팅 로직 추가, `pricing` 화면을 `trialStart`로 교체(카피는 3장 1단계 참고).
5. 홈 화면에 체험 상태 배너(2·3단계 카피) 추가.
6. `MyPageScreen.tsx`에 구독 관리 섹션 추가 (6단계).
7. Polar 웹훅 이벤트 중 `subscription.updated`(체험→active 전환, 취소 예약 등)도 처리 대상에 포함되어 있는지 재확인 — 지금 스위치문엔 없음.

이 문서 다음 단계로 실제 구현(스키마 마이그레이션, App.tsx 게이팅, 화면 컴포넌트)을 진행할지 알려주면 이어서 작업하겠다.
